require "simplecov"
SimpleCov.start

require_relative "spec_helper"

describe "Block class" do
  describe "initialize" do
    before do
    end

    # reservation is the PARENT
    it "creates a reservation for a room as an instance of a block" do
    end

    it "has a check in date and a check out date and stores the date range in an array" do
    end

    it "gives a default status of available" do
    end

    it "gives the block a name" do
    end
  end
end
